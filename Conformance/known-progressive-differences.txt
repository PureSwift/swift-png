# Progressive decodes the comparison reports but does not fail on.
#
# Same shape as the files next door, and the same reason for existing: a difference that has been
# examined is worth recording, and a list that mixed those with work not yet done would let the second
# look like the first.
#
# A line may begin with a guard naming the reference versions it is a claim about, as in
# "[>1.6.39] case reason".  An entry with no guard is a claim about every reference.  A guard that
# does not hold makes the entry inactive: it neither exempts a difference nor counts as stale,
# because it is not saying anything about the reference in the room.  See scripts/known_differences.sh.
#
# Format: <image> <reason>

[>1.6.39] meta-palette-hist.png The reference's reader refuses hIST wherever it appears, which is recorded for decoding in known-differences.txt where the reasoning is set out.
